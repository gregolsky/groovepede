# SQLite cache + Let's Encrypt certs — never overwritten by a deploy.
data/

# Resolver runtime config (DOMAIN, GP_PUBLIC_KEY, ALLOWED_ORIGINS, …)
.env

# Deploy-only config (holds the SSH target); must not land on the Pi.
deploy.env

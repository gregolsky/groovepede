node_modules/
dist/
infra/resolver/.build/
infra/resolver-pi/data/
infra/resolver-pi/.env
infra/resolver-pi/deploy.env
test-results/
playwright-report/

# Local env overrides — never commit (may contain secrets like VITE_GP_TOKEN)
.env.local
.env.*.local

# Claude Code: user-local settings and scratch worktrees
.claude/settings.local.json
.claude/worktrees/

# Personal palette reference (not for the build)
public/pallette.txt
